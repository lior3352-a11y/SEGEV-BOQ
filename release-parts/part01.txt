<!doctype html>
<html lang="he" dir="rtl">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>SEGEV BOQ - Professional Demo</title>
  <style>
    :root {
      --ink: #101828;
      --nav: #152033;
      --muted: #667085;
      --line: #e4e7ec;
      --paper: #f6f8fb;
      --panel: #ffffff;
      --gold: #d9952f;
      --gold-soft: #fff3df;
      --blue: #2364aa;
      --blue-soft: #eaf3ff;
      --red: #c83c3c;
      --red-soft: #fff1f1;
      --green: #16845b;
      --green-soft: #e9f8f1;
      --shadow: 0 18px 55px rgba(16, 24, 40, 0.08);
    }

    * { box-sizing: border-box; }

    body {
      margin: 0;
      min-height: 100vh;
      font-family: Arial, "Segoe UI", sans-serif;
      background: var(--paper);
      color: var(--ink);
      letter-spacing: 0;
    }

    button, input, textarea { font: inherit; }
    button { cursor: pointer; }

    .app {
      min-height: 100vh;
      display: grid;
      grid-template-columns: 264px minmax(0, 1fr);
    }

    .sidebar {
      background: var(--nav);
      color: #fff;
      padding: 28px 22px;
      display: flex;
      flex-direction: column;
      gap: 28px;
    }

    .brand {
      display: grid;
      grid-template-columns: 52px 1fr;
      gap: 14px;
      align-items: center;
    }

    .mark {
      width: 52px;
      height: 52px;
      border-radius: 8px;
      background: var(--gold);
      color: #17110a;
      display: grid;
      place-items: center;
      font-weight: 900;
      font-size: 22px;
    }

    .brand strong { display: block; font-size: 20px; }
    .brand span { display: block; margin-top: 5px; color: #b9c3d3; font-size: 12px; line-height: 1.45; }

    .nav {
      display: grid;
      gap: 6px;
    }

    .nav button {
      width: 100%;
      border: 0;
      border-radius: 8px;
      background: transparent;
      color: #ccd6e5;
      text-align: right;
      padding: 13px 14px;
      display: flex;
      justify-content: space-between;
      gap: 12px;
    }

    .nav button:hover,
    .nav button.active {
      background: rgba(255, 255, 255, 0.1);
      color: #fff;
    }

    .side-insight {
      margin-top: auto;
      border: 1px solid rgba(255, 255, 255, 0.14);
      background: rgba(255, 255, 255, 0.08);
      border-radius: 8px;
      padding: 16px;
      display: grid;
      gap: 10px;
    }

    .side-insight p {
      margin: 0;
      color: #d8dfec;
      line-height: 1.55;
      font-size: 13px;
    }

    .main {
      padding: 30px;
      display: grid;
      gap: 22px;
      align-content: start;
    }

    .topbar {
      min-height: 76px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 18px;
    }

    h1, h2, h3, p { margin-top: 0; }
    h1 { margin-bottom: 7px; font-size: 30px; line-height: 1.2; }
    h2 { margin-bottom: 8px; font-size: 22px; }
    h3 { margin-bottom: 12px; font-size: 18px; }
    p { color: var(--muted); line-height: 1.55; }

    .toolbar {
      display: flex;
      gap: 10px;
      flex-wrap: wrap;
      justify-content: flex-end;
    }

    .btn {
      border: 1px solid var(--line);
      background: #fff;
      color: var(--ink);
      border-radius: 8px;
      padding: 11px 15px;
      font-weight: 700;
      min-height: 42px;
    }

    .btn.primary { background: var(--gold); border-color: var(--gold); color: #17110a; }
    .btn.dark { background: var(--ink); border-color: var(--ink); color: #fff; }
    .btn.ghost { background: transparent; }

    .grid {
      display: grid;
      grid-template-columns: minmax(0, 1fr) 380px;
      gap: 22px;
      align-items: start;
    }

    .stack { display: grid; gap: 22px; }

    .metrics {
      display: grid;
      grid-template-columns: repeat(4, minmax(0, 1fr));
      gap: 14px;
    }

    .card {
      background: var(--panel);
      border: 1px solid var(--line);
      border-radius: 8px;
      box-shadow: var(--shadow);
      padding: 20px;
      min-width: 0;
    }

    .metric {
      min-height: 122px;
      display: grid;
      align-content: space-between;
      gap: 14px;
    }

    .label {
      color: var(--muted);
      font-size: 13px;
    }

    .number {
      font-size: 28px;
      font-weight: 900;
      line-height: 1;
    }

    .delta { color: var(--muted); font-size: 13px; }
    .delta.bad { color: var(--red); }
    .delta.good { color: var(--green); }

    .panel-head {
      display: flex;
      align-items: flex-start;
      justify-content: space-between;
      gap: 16px;
      margin-bottom: 14px;
    }

    .projects {
      display: grid;
      gap: 12px;
    }

    .project {
      border: 1px solid var(--line);
      background: #fff;
      border-radius: 8px;
      padding: 16px;
      display: grid;
      grid-template-columns: 1.5fr 1fr 1fr 1fr auto;
      gap: 14px;
      align-items: center;
    }

    .project strong { display: block; margin-bottom: 6px; }

    .pill {
      display: inline-flex;
      align-items: center;
      width: max-content;
      max-width: 100%;
      border-radius: 999px;
      padding: 6px 10px;
      font-size: 12px;
      font-weight: 800;
      white-space: nowrap;
    }

    .pill.green { background: var(--green-soft); color: var(--green); }
    .pill.gold { background: var(--gold-soft); color: #8a540d; }
    .pill.red { background: var(--red-soft); color: var(--red); }
    .pill.blue { background: var(--blue-soft); color: var(--blue); }

    .progress {
      height: 8px;
      border-radius: 999px;
      background: #edf1f6;
      overflow: hidden;
    }

    .progress span {
      display: block;
      height: 100%;
      border-radius: inherit;
      background: var(--blue);
    }

    .two {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 16px;
    }

    .risk-row {
      display: grid;
      grid-template-columns: 86px 1fr 70px;
      align-items: center;
      gap: 12px;
      margin: 14px 0;
    }

    .risk-bar {
      height: 12px;
      border-radius: 999px;
      background: #edf1f6;
      overflow: hidden;
    }

    .risk-bar span { display: block; height: 100%; border-radius: inherit; }
    .risk-bar.red span { background: var(--red); }
    .risk-bar.gold span { background: var(--gold); }
    .risk-bar.blue span { background: var(--blue); }

    .timeline {
      display: grid;
      gap: 12px;
    }

    .timeline-item {
      display: grid;
      grid-template-columns: 128px minmax(0, 1fr) 86px;
      gap: 14px;
      align-items: center;
      border: 1px solid var(--line);
      background: #fff;
      border-radius: 8px;
      padding: 14px;
    }

    .timeline-track {
      height: 12px;
      background: #edf1f6;
      border-radius: 999px;
      overflow: hidden;
    }

    .timeline-track span {
      display: block;
      height: 100%;
      border-radius: inherit;
    }

    .timeline-track.done span { background: var(--green); }
    .timeline-track.warn span { background: var(--gold); }
    .timeline-track.late span { background: var(--red); }

    .field-grid {
      display: grid;
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 14px;
    }

    .field-note {
      min-height: 170px;
      display: grid;
      align-content: space-between;
      gap: 12px;
      border: 1px solid var(--line);
      background: #fff;
      border-radius: 8px;
      padding: 16px;
    }

    .photo-placeholder {
      height: 92px;
      border-radius: 8px;
      background:
        linear-gradient(135deg, rgba(35, 100, 170, 0.12), rgba(217, 149, 47, 0.16)),
        repeating-linear-gradient(45deg, #f2f5f9, #f2f5f9 8px, #e8edf4 8px, #e8edf4 16px);
      border: 1px solid var(--line);
      display: grid;
      place-items: center;
      color: var(--muted);
      font-size: 13px;
      font-weight: 700;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      font-size: 14px;
    }

    th, td {
      padding: 12px 8px;
      border-bottom: 1px solid var(--line);
      text-align: right;
      vertical-align: middle;
    }

    th { color: var(--muted); font-size: 12px; }

    .ai {
      position: sticky;
      top: 24px;
      display: grid;
      gap: 14px;
      background: #fff;
      border: 1px solid var(--line);
      border-radius: 8px;
      padding: 20px;
      box-shadow: var(--shadow);
    }

    .ai-head {
      display: grid;
      grid-template-columns: 1fr 64px;
      gap: 14px;
      align-items: center;
    }

    .mic {
      width: 64px;
      height: 64px;
      border-radius: 50%;
      border: 0;
      background: var(--ink);
      color: #fff;
      font-size: 26px;
      display: grid;
      place-items: center;
    }

    .mic.listening {
      background: var(--gold);
      color: #17110a;
      box-shadow: 0 0 0 7px rgba(217, 149, 47, 0.18);
    }

    .ai textarea {
      min-height: 82px;
      resize: vertical;
      border: 1px solid var(--line);
      border-radius: 8px;
      padding: 13px;
      outline: none;
      line-height: 1.5;
    }

    .answer {
      min-height: 160px;
      border-radius: 8px;
      border: 1px solid #cfe1f5;
      background: var(--blue-soft);
      padding: 16px;
      line-height: 1.7;
    }

    .quick {
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
    }

    .quick button {
      border: 1px solid var(--line);
      background: #fff;
      border-radius: 999px;
      padding: 8px 11px;
      color: var(--ink);
      font-size: 12px;
    }

    .actions-list {
      display: grid;
      gap: 8px;
    }

    .action-item {
      border: 1px solid var(--line);
      background: #fff;
      border-radius: 8px;
      padding: 12px;
      display: grid;
      grid-template-columns: 1fr auto;
      gap: 10px;
      align-items: center;
    }

    .site-weather {
      display: grid;
      grid-template-columns: 72px 1fr;
      gap: 14px;
      align-items: center;
      border: 1px solid #fde0b6;
      background: var(--gold-soft);
      border-radius: 8px;
      padding: 16px;
      margin-top: 14px;
    }

    .weather-temp {
      width: 72px;
      height: 72px;
      border-radius: 8px;
      display: grid;
      place-items: center;
      background: #fff;
      color: #8a540d;
      font-size: 24px;
      font-weight: 900;
    }

    .log-list {
      display: grid;
      gap: 8px;
    }

    .log-item {
      border: 1px solid var(--line);
      background: #fff;
      border-radius: 8px;
      padding: 11px 12px;
      color: var(--muted);
      line-height: 1.45;
      font-size: 13px;
    }

    .learning {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 12px;
    }

    .view { display: none; }
    .view.active { display: block; }

    .modal {
      position: fixed;
      inset: 0;
      background: rgba(16, 24, 40, 0.45);
      display: none;
      place-items: center;
      padding: 24px;
      z-index: 20;
    }

    .modal.open { display: grid; }

    .dialog {
      width: min(620px, 100%);
      background: #fff;
      border-radius: 8px;
      box-shadow: 0 24px 90px rgba(16, 24, 40, 0.28);
      padding: 24px;
      display: grid;
      gap: 16px;
    }

    .form {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 14px;
    }

    label { display: grid; gap: 7px; color: var(--muted); font-size: 13px; }
    input {
      border: 1px solid var(--line);
      border-radius: 8px;
      padding: 12px;
      color: var(--ink);
    }

    .toast {
      position: fixed;
      left: 24px;
      bottom: 24px;
      display: none;
      background: var(--ink);
      color: #fff;
      border-radius: 8px;
      padding: 12px 16px;
      z-index: 30;
      box-shadow: var(--shadow);
    }

    .toast.show { display: block; }

    .demo-guide {
      position: fixed;
      right: 292px;
      bottom: 24px;
      width: min(420px, calc(100vw - 48px));
      background: #fff;
      border: 1px solid var(--line);
      border-radius: 8px;
      box-shadow: 0 24px 80px rgba(16, 24, 40, 0.18);
      padding: 18px;
      display: none;
      z-index: 25;
    }

    .demo-guide.open { display: grid; gap: 12px; }

    .demo-guide h3 { margin: 0; }

    .demo-guide p { margin: 0; }

    .demo-step-count {
      color: var(--muted);
      font-size: 12px;
      font-weight: 800;
    }

    .demo-guide .toolbar { justify-content: space-between; }

    @media (max-width: 1220px) {
      .grid { grid-template-columns: 1fr; }
      .ai { position: static; }
      .metrics { grid-template-columns: repeat(2, 1fr); }
    }

    @media (max-width: 860px) {
      .app { grid-template-columns: 1fr; }
      .sidebar { padding: 22px; }
      .nav { grid-template-columns: repeat(2, 1fr); }
      .topbar { flex-direction: column; align-items: stretch; }
      .toolbar { justify-content: stretch; }
      .toolbar .btn { flex: 1; }
      .project, .two, .form, .learning { grid-template-columns: 1fr; }
      .metrics { grid-template-columns: 1fr; }
      .demo-guide { right: 24px; }
    }

    body::before {
      content: "";
      position: fixed;
      inset: 0;
      pointer-events: none;
      background:
        linear-gradient(180deg, rgba(255, 255, 255, 0.72), rgba(246, 248, 251, 0.92)),
        repeating-linear-gradient(0deg, rgba(16, 24, 40, 0.026), rgba(16, 24, 40, 0.026) 1px, transparent 1px, transparent 46px);
      z-index: -1;
    }

    .sidebar {
      min-height: 100vh;
      position: sticky;
      top: 0;
    }

    .nav {
      max-height: calc(100vh - 260px);
      overflow: auto;
      padding-left: 4px;
    }

    .nav button {
      align-items: center;
      transition: background 160ms ease, color 160ms ease, transform 160ms ease;
    }

    .nav button.active {
      box-shadow: inset 3px 0 0 var(--gold);
    }

    .nav button:hover {
      transform: translateX(-2px);
    }

    .main {
      padding: 34px;
    }

    .topbar {
      min-height: 88px;
      background: rgba(255, 255, 255, 0.9);
      border: 1px solid var(--line);
      border-radius: 8px;
      padding: 20px 22px;
      box-shadow: var(--shadow);
    }

    .btn {
      transition: transform 150ms ease, box-shadow 150ms ease, background 150ms ease;
    }

    .btn:hover {
      transform: translateY(-1px);
      box-shadow: 0 10px 22px rgba(16, 24, 40, 0.09);
    }

    .view.active {
      animation: viewIn 180ms ease;
    }

    @keyframes viewIn {
      from { opacity: 0; transform: translateY(6px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .metric {
      position: relative;
      overflow: hidden;
    }

    .metric::before {
      content: "";
      position: absolute;
      inset: 0 auto 0 0;
      width: 4px;
      background: var(--gold);
    }

    .project {
      transition: border-color 150ms ease, box-shadow 150ms ease, transform 150ms ease;
    }

    .project:hover {
      border-color: #c9d3e2;
      box-shadow: 0 12px 34px rgba(16, 24, 40, 0.08);
      transform: translateY(-1px);
    }

    table {
      background: #fff;
      border-radius: 8px;
      overflow: hidden;
    }

    th {
      background: #f8fafc;
    }

    tbody tr:hover {
      background: #fbfcff;
    }

    .ai {
      border-top: 4px solid var(--gold);
    }

    .answer {
      color: #17324d;
    }

    .premium-band {
      position: relative;
      overflow: hidden;
      background:
        linear-gradient(135deg, rgba(16, 24, 40, 0.98), rgba(21, 32, 51, 0.92)),
        radial-gradient(circle at 18% 18%, rgba(217, 149, 47, 0.28), transparent 34%);
      color: #fff;
      border: 0;
      min-height: 154px;
    }

    .premium-band::after {
      content: "";
      position: absolute;
      inset: auto 0 0;
      height: 5px;
      background: linear-gradient(90deg, var(--gold), #f3c36c, var(--blue));
    }

    .premium-band p,
    .premium-band .label {
      color: #d8dfec;
    }

    .premium-band h2 {
      font-size: 28px;
      margin-bottom: 8px;
    }

    .command-strip {
      display: grid;
      grid-template-columns: repeat(4, minmax(0, 1fr));
      gap: 12px;
      margin-top: 18px;
    }

    .command-chip {
      border: 1px solid rgba(255, 255, 255, 0.14);
      background: rgba(255, 255, 255, 0.08);
      border-radius: 8px;
      padding: 12px;
      display: grid;
      gap: 6px;
    }

    .command-chip strong {
      font-size: 18px;
    }

    .metric {
      background:
        linear-gradient(180deg, rgba(255, 255, 255, 0.98), rgba(255, 255, 255, 0.9)),
        linear-gradient(135deg, rgba(35, 100, 170, 0.08), rgba(217, 149, 47, 0.08));
    }

    .number {
      color: #0f1f36;
    }

    .card {
      backdrop-filter: blur(10px);
    }

    .brand .mark {
      box-shadow: 0 18px 34px rgba(217, 149, 47, 0.28);
    }

    .side-insight {
      box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.08);
    }

    .ai-head h2::after,
    .panel-head h2::after {
      content: "";
      display: block;
      width: 42px;
      height: 3px;
      margin-top: 9px;
      border-radius: 999px;
      background: var(--gold);
    }

    @media (max-width: 1180px) {
      .command-strip {
        grid-template-columns: repeat(2, minmax(0, 1fr));
      }
    }

    @media (max-width: 720px) {
      .command-strip {
        grid-template-columns: 1fr;
      }
    }
  
    /* SEGEV commercial access layer */
    .auth-gate{position:fixed;inset:0;z-index:1000;background:#f3f5f8;display:grid;place-items:center;padding:24px;direction:rtl}
    .auth-gate.hidden{display:none}.auth-shell{width:min(560px,100%);background:#fff;border:1px solid var(--line);border-radius:12px;box-shadow:var(--shadow);overflow:hidden}
    .auth-brand{background:var(--nav);color:#fff;padding:24px 28px;display:flex;align-items:center;gap:14px}.auth-brand .mark{flex:0 0 52px}.auth-brand p{color:#c9d2df;margin:5px 0 0}
    .auth-body{padding:26px}.auth-tabs{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-bottom:20px}.auth-t